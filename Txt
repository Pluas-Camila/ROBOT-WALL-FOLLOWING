Class 2 

Another way to PID
Function (distance)
inititalization 
start_reading  => current_encoder_readings
while loop:


1. Meassure:
  curr_reading -=> current encoder readings 
  acceleration = radiusofW(curr_reading - start_reading)

2. Send Control: 
  Phi = Kp(distance - acceleration)

3. Check:
  Set both Left and Right Motores Set (Phi)

4. Stop / Break:
  If distance - change in distance <= a <= distance +change in distance:
    STOP
    break return 1




-==========================================================================================-

Function2 rotation input angle can be positive and negative 
{
initializitation currentreading ofIMU 

  if angle is negative then we are going clockwise 
  if notwe are going counter cw

  end_heading = (curr_heading + angle ) mod 360 or 2pi
  
  end_heading is our desired(d) from the equation  e(t) =  d-a

  e(t)= | end_heading - IMU |   //absolute value 
  Phi= Kp* e(t)
  if CW  <0
    L = Phi (positive)
    R = -Phi(negative)    
  else CCW >0
    R = Phi (positive)
    L = -Phi(negative) 


  if end-heading - change /_\ < = IMU <= end_heading + change
    break
    stop








}
